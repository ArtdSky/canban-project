@baseUrl = http://localhost:8080
@token = your-token-here
@taskId = 1

### Обновить задачу
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "title": "Обновленное название",
  "description": "Обновленное описание",
  "status": "in_progress"
}

### Обновить список исполнителей
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "assignee_ids": [2, 3, 5]
}

### Обновить список наблюдателей
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "observer_ids": [4, 6, 7]
}

### Обновить исполнителей и наблюдателей одновременно
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "title": "Обновленная задача",
  "assignee_ids": [2, 3],
  "observer_ids": [4]
}

### Очистить список исполнителей
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "assignee_ids": []
}

### Изменить статус задачи с todo на done (проверка state machine)
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "status": "done"
}

### Изменить статус задачи с done на todo (проверка state machine - недопустимый переход)
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "status": "todo"
}

### Обновить задачу с невалидным статусом (проверка валидации)
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "status": "invalid_status"
}

### Обновить задачу, к которой нет доступа (должна быть ошибка)
PUT {{baseUrl}}/api/tasks/{{taskId}}
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "title": "Название"
}

